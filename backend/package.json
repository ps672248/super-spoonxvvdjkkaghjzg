{
  "name": "aspirant-arcade-backend",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Minimal backend for the shared question bank — validated writes + reports via Firebase Admin.",
  "main": "src/index.js",
  "scripts": {
    "start": "node src/index.js",
    "dev": "node --watch src/index.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.19.2",
    "firebase-admin": "^12.0.0"
  }
}
