Drop these files here (see `marketing/video/README.md` "Audio" section for sourcing + length guidance):

- `quiz.mp3` — QuizCard background bed (plays once, no loop)
- `bgm.mp3` — NewsRecap background bed (loops)
- `tick.mp3` — countdown sound, ~3s
- `reveal.mp3` — answer-reveal chime, ≤1s
- `outro.mp3` — plays once at the start of the CTA outro, shared by both QuizCard and NewsRecap

All optional — `render.ts` only wires in whichever files actually exist.
